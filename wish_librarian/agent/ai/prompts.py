"""
Промпты для AI-клиентов.

Содержит:
  - системные промпты для разных задач (для обратной совместимости)
  - функции для сборки пользовательских промптов с контекстом книги
  - ``build_style_directive`` — единый блок «стиль письма» для system prompt
  - ``build_system_prompt`` — собирает system prompt из шаблона + style

DEPRECATED: ``SUMMARY_SYSTEM_PROMPT``, ``WORKBOOK_SYSTEM_PROMPT``,
``TIPS_SYSTEM_PROMPT`` оставлены как fallback, если у шаблона пустой
``system_prompt``. В новом коде используйте ``build_system_prompt``.
"""
from __future__ import annotations

from agent.models import BookInfo, ReviewBundle, ScientificArticle
from agent.templates import ContentTemplate


# ── Системные промпты (DEPRECATED, оставлены для обратной совместимости) ──

SUMMARY_SYSTEM_PROMPT = """\
Ты — профессиональный редактор-аналитик, который делает глубокие, \
практико-ориентированные конспекты нон-фикшн книг по саморазвитию, \
психологии, исполнению желаний, трансерфингу, духовным практикам и смежным темам.

Твоя задача — превратить сырой материал в МАСТЕРСКИЙ КОНСПЕКТ, который:
1. Передаёт главный посыл книги одним предложением.
2. Выделяет 3–5 ключевых концепций (не пересказ, а суть).
3. Даёт 3–5 точных цитат (если они есть во входных данных).
4. Содержит 3–5 практических выводов.
5. Включает пошаговую дорожную карту внедрения на 30 дней.

Пиши на русском, ясно, без воды. Используй markdown-разметку и эмодзи, \
как указано в шаблоне. Никакой цензуры и пропуска «опасных» тем — \
это материалы по саморазвитию, они по определению безопасны.
"""


WORKBOOK_SYSTEM_PROMPT = """\
Ты — коуч-практик, который превращает идеи книг в работающие упражнения. \
Создай воркбук (рабочую тетрадь) с практическими заданиями.

Требования к воркбуку:
- Упражнение 1. Самоанализ: 5–7 сильных вопросов, которые помогают \
  читателю осознать свои текущие паттерны.
- Упражнение 2. Практика: 3–5 конкретных действий, которые можно \
  выполнить СЕГОДНЯ.
- Упражнение 3. Планирование: шаблон 30-дневного плана внедрения.
- Рефлексия: 5–7 вопросов для итогового осмысления через 30 дней.

Пиши на русском. Используй markdown-разметку. Делай упражнения \
конкретными, не абстрактными. Каждый вопрос должен «цеплять».
"""


TIPS_SYSTEM_PROMPT = """\
Ты — практик-консультант, который превращает идеи книг в \
короткие практические советы на каждый день.

Сгенерируй 10–15 практических советов (1–2 предложения каждый), \
которые человек может начать применять сразу после прочтения конспекта. \
Советы должны быть:
- Конкретными (что именно делать).
- Применимыми сегодня.
- Связанными с ключевыми идеями книги.
- Безопасными.

Пиши на русском. Markdown-список.
"""


# Карта kind → дефолтный DEPRECATED system prompt (если у шаблона пусто)
_FALLBACK_SYSTEM_PROMPTS = {
    "summary": SUMMARY_SYSTEM_PROMPT,
    "workbook": WORKBOOK_SYSTEM_PROMPT,
    "tips": TIPS_SYSTEM_PROMPT,
}


# ── Style directive (приклеивается к system prompt) ─────────────────
def build_style_directive(
    tone: str,
    length: str,
    audience: str,
    language: str,
) -> str:
    """
    Сгенерировать блок «СТИЛЬ ПИСЬМА» для system prompt.

    Значения принимаются в любом регистре; на выходе — нормализованные.
    """
    t = (tone or "coaching").lower()
    ln = (length or "medium").lower()
    au = (audience or "general").lower()
    lang = (language or "ru").lower()

    return (
        "\n\n## СТИЛЬ ПИСЬМА (обязательно)\n"
        f"- Тон: {t.upper()}\n"
        f"- Длина: {ln.upper()}\n"
        f"- Аудитория: {au.upper()}\n"
        f"- Язык: {lang}\n"
        "\nИнтерпретация:\n"
        "  • FORMAL  — академично, без сленга, минимум эмодзи.\n"
        "  • CASUAL  — разговорно, дружелюбно, можно 'ты'.\n"
        "  • COACHING — мотивирующе, 'ты', с призывами к действию.\n"
        "  • SHORT/MEDIUM/LONG — коротко/средне/развёрнуто (примерный объём).\n"
        "  • GENERAL/EXPERT/TEEN — поправь лексику и глубину.\n"
    )


def build_system_prompt(
    tpl: ContentTemplate,
    *,
    tone: str = "coaching",
    length: str = "medium",
    audience: str = "general",
    language: str = "ru",
) -> str:
    """
    Собрать system prompt для AI:

    1. ``tpl.system_prompt`` (если есть в шаблоне) → иначе fallback
       ``SUMMARY_SYSTEM_PROMPT`` / ``WORKBOOK_SYSTEM_PROMPT`` / ``TIPS_SYSTEM_PROMPT``.
    2. Приклеить style directive.
    """
    base = (tpl.system_prompt or "").strip()
    if not base:
        base = _FALLBACK_SYSTEM_PROMPTS.get(tpl.kind, "").strip()
    return base + build_style_directive(tone, length, audience, language)


# ── Сборка пользовательских промптов ─────────────────────────────

def _book_variables(book: BookInfo) -> dict:
    """Общие переменные для подстановки в шаблон."""
    return {
        "title": book.title,
        "author": book.author or "—",
        "year": str(book.year) if book.year else "—",
        "source_url": book.source_url,
        "isbn": book.isbn or "—",
    }


def render_user_prompt(tpl: ContentTemplate, book: BookInfo, **extra) -> str:
    """
    Подставить переменные книги (+ ``extra``) в тело шаблона и вернуть
    готовый user prompt для отправки в LLM.
    """
    from agent.templates import render_body  # local import — avoids cycle
    vars_ = {**_book_variables(book), **extra}
    return render_body(tpl, vars_)


def build_summary_prompt(book: BookInfo) -> str:
    """DEPRECATED: ручной шаблон. Используйте ``render_user_prompt`` + шаблон."""
    chapters_txt = (
        "\n".join(f"- Глава {c.number}. {c.title}" for c in book.chapters[:30])
        or "— не указаны —"
    )
    ideas_txt = (
        "\n".join(f"- {i}" for i in book.key_ideas[:10]) or "— не указаны —"
    )
    quotes_txt = (
        "\n".join(f"- {q}" for q in book.quotes[:5]) or "— не указаны —"
    )
    desc = book.short_description or "—"
    cover = f"\n[URL обложки]: {book.cover_url}" if book.cover_url else ""

    return f"""\
Сделай МАСТЕР-КОНСПЕКТ по следующей книге.

═══════════════════════════════════════
📕 НАЗВАНИЕ: {book.title}
✍️  АВТОР: {book.author}
📅 ГОД: {book.year or '—'}
🔗 ИСТОЧНИК: {book.source_url}{cover}
═══════════════════════════════════════

📝 КРАТКОЕ ОПИСАНИЕ:
{desc}

💡 КЛЮЧЕВЫЕ ИДЕИ (из парсера):
{ideas_txt}

💬 ЦИТАТЫ (из парсера):
{quotes_txt}

📑 СТРУКТУРА КНИГИ (главы):
{chapters_txt}

═══════════════════════════════════════

Верни результат СТРОГО в следующем markdown-шаблоне (заполни все секции):

# 📚 КОНСПЕКТ: {book.title}

## 📋 Общая информация
- **Автор:** {book.author}
- **Год:** {book.year or '—'}
- **Жанр:** саморазвитие / практическая психология

> **Ключевая идея:** (одно ёмкое предложение, отражающее главный посыл)

## 🎯 Главные концепции
(3–5 концепций, каждая — название + 2–4 предложения сути)

## 💬 Ключевые цитаты
(3–5 цитат, формат: «текст» — автор)

## ✅ Основные выводы
(3–5 практических выводов)

## 🧭 Дорожная карта внедрения
Пошаговый план на 30 дней:
- **Неделя 1:** …
- **Неделя 2:** …
- **Неделя 3:** …
- **Неделя 4:** …

## 🔗 Связанные идеи
(2–3 идеи из смежных областей, которые усиливают книгу)

---
_Сгенерировано WishLibrarian • {book.source_url}_
"""


def build_workbook_prompt(book: BookInfo, summary_md: str = "") -> str:
    """DEPRECATED: ручной шаблон. Используйте ``render_user_prompt`` + шаблон."""
    ideas = "\n".join(f"- {i}" for i in book.key_ideas[:7]) or "—"
    return f"""\
Создай ВОРКБУК (рабочую тетрадь) для книги:

📕 {book.title} — {book.author}

Ключевые идеи:
{ideas}

{f"Краткое содержание (конспект):\\n{summary_md[:3000]}" if summary_md else ""}

═══════════════════════════════════════

Верни результат СТРОГО в этом шаблоне (заполни все секции):

# ✍️ ВОРКБУК: {book.title}

> _«{book.title}» — практическая рабочая тетрадь_

## 🔍 Упражнение 1. Самоанализ
Ответь письменно (5–7 вопросов, каждый пронумерован):
1. …
2. …

## ✍️ Упражнение 2. Практика
Сделай СЕГОДНЯ (3–5 действий):
- [ ] …
- [ ] …

## 📝 Упражнение 3. Планирование
Заполни шаблон 30-дневного плана:

| Неделя | Цель | 3 ключевых действия | Ожидаемый результат |
|--------|------|---------------------|----------------------|
| 1      |      |                     |                      |
| 2      |      |                     |                      |
| 3      |      |                     |                      |
| 4      |      |                     |                      |

## 💭 Рефлексия (через 30 дней)
Ответь заново:
1. Что изменилось?
2. Что далось легче всего?
3. Что оказалось самым сложным?
4. Какие открытия сделал о себе?
5. Что буду делать дальше?

## 🎁 Бонус: ежедневные микро-привычки
- …
- …

---
_Сгенерировано WishLibrarian_
"""


def build_tips_prompt(book: BookInfo, summary_md: str = "") -> str:
    """DEPRECATED: ручной шаблон. Используйте ``render_user_prompt`` + шаблон."""
    return f"""\
Сгенерируй PRACTICAL_TIPS — 10–15 коротких практических советов \
на каждый день по книге «{book.title}» ({book.author}).

{f"Конспект:\\n{summary_md[:2500]}" if summary_md else ""}

Верни результат в формате:

# 💡 Практические советы: {book.title}

> _Короткие действия, которые работают уже сегодня_

## 🗓 Ежедневная рутина
(советы 1–5, 1–2 предложения каждый)

## ⚡ «Быстрые победы» (до 5 минут)
(советы 6–10)

## 🌙 Вечерние практики
(советы 11–15)

---
_Сгенерировано WishLibrarian_
"""
