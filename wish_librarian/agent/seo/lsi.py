"""
LSI-база для ниши «Саморазвитие / исполнение желаний».
Портировано из C:/Users/kfigh/seo-advisor-skill/data/lsibase.md.
"""
from __future__ import annotations

from typing import List, Dict


# Универсальные LSI для саморазвития
UNIVERSAL = [
    'осознанность', 'привычки', 'мотивация', 'дисциплина', 'цель',
    'приоритеты', 'ценности', 'убеждения', 'мышление', 'рефлексия',
    'дневник', 'практика', 'результат', 'прогресс', 'страх',
    'прокрастинация',
]

# Исполнение желаний / трансерфинг
TRANSCENDING = [
    'визуализация', 'аффирмация', 'медитация', 'карта желаний',
    'намерение', 'подсознание', 'энергия', 'квантовое поле',
    'закон притяжения', 'благодарность', 'поток', 'вселенная',
    'сценарий жизни', 'маятник', 'слайды', 'транс',
    'внешнее намерение', 'дуальное расщепление', 'квантовый переход',
    'пространство вариантов', 'излучение', 'важность', 'равновесие',
    'гармония', 'отпускание', 'принятие',
]

# Психология / терапия
PSYCHOLOGY = [
    'эмоции', 'чувства', 'травма', 'детство', 'родители', 'отношения',
    'границы', 'привязанность', 'самооценка', 'самопринятие', 'терапия',
    'гештальт', 'когнитивно-поведенческая', 'психоанализ', 'эмпатия',
    'ассертивность', 'контакт', 'зрелость',
]

# Книги / библиотека
BOOKS = [
    'аннотация', 'рецензия', 'отзыв', 'воркбук', 'конспект', 'саммари',
    'идеи', 'цитаты', 'глава', 'автор', 'издание', 'жанр', 'нон-фикшн',
    'бестселлер',
]

# Маппинг жанр → LSI
GENRE_TO_LSI: Dict[str, List[str]] = {
    'трансерфинг': TRANSCENDING,
    'саморазвитие': UNIVERSAL + TRANSCENDING,
    'психология': PSYCHOLOGY,
    'эзотерика': TRANSCENDING,
    'духовность': TRANSCENDING + PSYCHOLOGY,
    'бизнес': UNIVERSAL + ['лидерство', 'стратегия', 'управление'],
    'финансы': UNIVERSAL + ['доход', 'инвестиции', 'капитал', 'бюджет'],
    'здоровье': UNIVERSAL + ['здоровье', 'сон', 'питание', 'движение', 'стресс'],
    'отношения': PSYCHOLOGY + ['любовь', 'партнер', 'семья', 'дети'],
}


def get_lsi_for_book(genres: List[str], max_n: int = 15) -> List[str]:
    """
    Вернуть LSI-слова для книги на основе её жанров.
    Дедупликация, ограничение по max_n.
    """
    if not genres:
        return UNIVERSAL[:max_n]

    seen = set()
    result: List[str] = []
    for genre in genres:
        genre_lower = genre.lower().strip()
        for key, words in GENRE_TO_LSI.items():
            if key in genre_lower or genre_lower in key:
                for w in words:
                    if w not in seen:
                        seen.add(w)
                        result.append(w)
                        if len(result) >= max_n:
                            return result
    # Если ничего не нашлось по жанру — fallback на универсальные
    if not result:
        return UNIVERSAL[:max_n]
    return result
