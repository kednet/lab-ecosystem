# experiments_bot — env (НЕ коммитить)
# Минимум для запуска:
TELEGRAM_BOT_TOKEN=

# Прокси (тот же, что у WL-ботов на VPS). Если пусто — прямой запрос в TG (не сработает из РФ).
TELEGRAM_PROXY_URL=

# API Лаборатории для отправки экспериментов и получения списка книг
EXPERIMENTS_API_URL=https://api.pulab.online
BOOKS_API_URL=https://app.pulab.ru

# Логи
LOG_LEVEL=INFO
